apiVersion: v1
kind: Service

metadata:
  name: loki
  namespace: logging

  labels:
    app.kubernetes.io/name: loki
    app.kubernetes.io/component: logging
    app.kubernetes.io/part-of: sentinelai

spec:
  selector:
    app.kubernetes.io/name: loki

  ports:
    - port: 3100
      targetPort: 3100

  type: ClusterIP