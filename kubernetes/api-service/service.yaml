apiVersion: v1
kind: Service

metadata:
  name: sentinel-api-service
  namespace: sentinel
  labels:
    app.kubernetes.io/name: sentinel-api

spec:
  selector:
    app.kubernetes.io/name: sentinel-api

  ports:
    - name: http
      port: 8000
      targetPort: 8000

  type: NodePort
